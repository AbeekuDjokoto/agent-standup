/** Root wrapper for dashboard pages — prevents horizontal overflow on mobile Safari. */
export const dashboardPageClassName =
  'min-h-[100dvh] min-w-0 bg-[#f9fafa] px-3 pb-[max(1rem,env(safe-area-inset-bottom))] pt-[max(0.75rem,env(safe-area-inset-top))] sm:px-4';

export const dashboardMainClassName = 'mx-auto mt-3 w-full max-w-7xl space-y-3 sm:mt-4 sm:space-y-4';
